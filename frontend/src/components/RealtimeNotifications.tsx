import React from 'react';

// All WebSocket notifications disabled per client request.
// Component kept mounted in App.tsx to avoid unnecessary refactoring.
export const RealtimeNotifications: React.FC = () => {
    return null;
};
